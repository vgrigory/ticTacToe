/**
 * We init TicTacToe module here.
 * (custom) bootstrapping of application happens in this file:
 * (loading of related resources - translation, ...).
 *
 */
var TicTacToeApp = angular.module('TicTacToe', ['ui.bootstrap']);